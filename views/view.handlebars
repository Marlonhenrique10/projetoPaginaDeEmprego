<div id="job-view-container" class="container">
    <div class="row text-center">
        <a class="back-link main-color" href="/">Voltar</a>
        <dic id="img-container" class="col-md-12">
            <img src="/src/foto-empresa.jpeg" alt="Compania">
        </dic>
        <div class="col-md-12">
            <h1 class="main-color">{{job.dataValues.title}}</h1>
            <p class="bold">Quem eles estão procurando: </p>
            <p class="job-desc">{{job.dataValues.description}}</p>
            <p><span class="bold">O salário é:</span> R${{job.dataValues.salary}}</p>
            <p>Para Trabalhar na <span class="bold main-color">{{job.dataValues.company}}</span> envie um e-mail para: <a class="main-color" href="mail:to{{job.dataValues.email}}">{{job.dataValues.email}}</a></p>
        </div>
    </div>
</div>